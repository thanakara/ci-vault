name: CI-basic

on:
    push:
        branches:
            # - main
            - feature/*

    workflow_dispatch:
    

jobs:
    draw:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4

          - name: Run the script
            run: |
                set -euo pipefail
                ls -la
                chmod +x misc/draw
                misc/draw
